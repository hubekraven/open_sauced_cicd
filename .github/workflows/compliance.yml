name: Compliance

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize
      - reopened
  workflow_call: {}

permissions:
  pull-requests: write

jobs:
  semantics:
    name: Semantics (simulation)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Simulate semantic pull request check
        run: echo "✅ Simulation : vérification sémantique de la PR réussie."

  pr-compliance-checks:
    name: PR Compliance Checks (simulation)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Simulate PR compliance action
        run: |
          echo "✅ Simulation : vérification compliance PR avec les fichiers package.json, package-lock.json, npm-shrinkwrap.json"
          echo "Message de vérification :"
          echo "In order to be considered for merging, the pull request description must refer to a specific issue number."
          echo "See contributing guide at https://docs.opensauced.pizza/contributing/introduction-to-contributing/"

  welcome:
    name: Welcome (simulation)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: semantics
    if: github.event.action == 'opened'
    steps:
      - name: Simulate welcome message on first PR
        run: echo "🎉 Simulation : message de bienvenue envoyé pour la première PR !"
